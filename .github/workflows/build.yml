name: Build Windows EXE

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    uses: fyjn4r5/dabao/.github/workflows/build-exe.yml@main
    with:
      script_name: 批量生成信件_260324.py
      exe_name: 批量生成信件
